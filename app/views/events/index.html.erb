<main>
  <h1 class="m-5">Event</h1>

  <div class="m-5 border border-success events">
    <%= @event.description %> <br>
    <% @event.attendances.each do |attendances| %>
    <div class="border">
      <%= User.find(attendances.user_id).name %> <br>
    </div>
    <% end %>
  </div>

</main>